<fieldset>
	<legend>Open</legend>
	
	<% @open_jobs.each do |i| %>
		<%= i.name %>
	<% end %>
	
</fieldset>

<fieldset>
	<legend>Closed</legend>

	<% @closed_jobs.each do |i| %>
		<%= i.name %>
	<% end %>

</fieldset>

<% #Method calls and global variable modifiers of the page %>

<% $active_tab = 2 %>
<% $active_page = "job.index"%>

